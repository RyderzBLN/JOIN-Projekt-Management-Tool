/**
 * @typedef {Object} FirebaseConfig
 * @property {string} apiKey - The Firebase Realtime Database API key.
 */

/**
 * The Firebase Realtime Database configuration.
 * @type {FirebaseConfig}
 */
const firebaseConfig = {
  /**
   * The Firebase Realtime Database API key.
   * @type {string}
   */
};
